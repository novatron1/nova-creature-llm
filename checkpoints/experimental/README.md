# Experimental Checkpoints

Place experimental or alternate checkpoint files here.

The v054 builder checks this directory as the second priority after `checkpoints/base/`.

Example:

```
checkpoints/experimental/creature_v032_bigfit_twenty_plain.pt
```
