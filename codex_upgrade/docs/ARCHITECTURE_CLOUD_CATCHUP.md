# Cloud Catch-Up Architecture

This cloud package recreates the proven Nova Creature architecture without assuming local Windows access.

Flow:

Input -> v052 role router -> brain role -> answer/routes

Training path:

pending lesson -> approved lesson -> export role training set -> build role checkpoint layer -> torch fine-tune readiness

Roles:

- left_hemisphere
- right_hemisphere
- memory_transformer
- planner_transformer
- critic_conscience_transformer
- dream_simulation_transformer
- speech_output_transformer
